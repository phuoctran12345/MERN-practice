{
  "name": "mern-hotel-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "MERN Hotel Booking Monorepo - Railway will use backend/ folder",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

